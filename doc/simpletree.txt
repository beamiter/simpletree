*simpletree.txt*        Asynchronous file tree for Vim 9

==============================================================================
CONTENTS                                                *simpletree-contents*

1. Introduction........................................|simpletree|
2. Requirements........................................|simpletree-requirements|
3. Installation........................................|simpletree-install|
4. Commands............................................|simpletree-commands|
5. Mappings............................................|simpletree-mappings|
6. Options.............................................|simpletree-options|
7. Persisted state.....................................|simpletree-width-state|
8. Backend diagnostics.................................|simpletree-backend|
9. File-operation semantics............................|simpletree-safety|
10. Tab pages..........................................|simpletree-tabpages|
11. Troubleshooting....................................|simpletree-troubleshooting|

==============================================================================
1. INTRODUCTION                                             *simpletree*

SimpleTree is a Vim 9 file tree backed by an asynchronous Rust directory
scanner.  It provides a collapsible workspace root, active-file reveal,
modified-buffer marks, path-stable selection, editor-window reuse, and file
operations.

The backend bounds concurrent blocking scans, emits chunked results, understands
Git ignore rules, and supports version and capability discovery.  The user
interface and messages are currently primarily Chinese.  SimpleTree uses Vim9
script and does not support Neovim.

==============================================================================
2. REQUIREMENTS                              *simpletree-requirements*

- Vim 9.0 or newer with +job, +channel, and +float.
- Rust 1.88 or newer and Cargo to build the backend.
- Bash for install.sh; native Windows installation is not currently covered.
- A Nerd Font and a system trash provider are optional.

Check Vim with: >

  :echo has('job')
  :echo has('channel')
  :echo has('float')
<
All three values must be 1.

==============================================================================
3. INSTALLATION                                  *simpletree-install*

VIM-PLUG ~
>
  call plug#begin()
  Plug 'beamiter/simpletree', { 'do': './install.sh' }
  call plug#end()
<
Then run :PlugInstall.  Use :PlugUpdate for later updates.

VIM NATIVE PACKAGE ~
>
  git clone https://github.com/beamiter/simpletree.git \
    ~/.vim/pack/plugins/start/simpletree
  ~/.vim/pack/plugins/start/simpletree/install.sh
<
install.sh resolves the repository from its own location, uses Cargo.lock,
pins an in-repository target directory and native Rust target, verifies the
new binary with --version, and atomically replaces only
lib/simpletree-daemon.

The plugin directory must be on 'runtimepath'.  SimpleTree searches each entry
under lib/ first, then target/release/ and target/debug/ for development use.

==============================================================================
4. COMMANDS                                      *simpletree-commands*

                                                     *:SimpleTree*
:SimpleTree [directory]
        Open or close the tree in the current tab page.  An optional directory
        becomes the root.  See |simpletree-tabpages|.

                                                     *:SimpleTreeRefresh*
:SimpleTreeRefresh
        Clear the tree cache and rescan.

                                                     *:SimpleTreeReveal*
:SimpleTreeReveal [path]
        Reveal the current active file, or an explicit file/directory below
        the existing root. A relative path is always resolved against the tree
        root, independent of :pwd or :lcd. Required ancestors are expanded
        asynchronously and only the newest reveal may move the selection.
        The lexical path and every ancestor after symlink resolution must stay
        inside the workspace, including paths that escape and later re-enter;
        missing or escaping paths leave selection, expansion state and render
        epoch unchanged. Completion enumerates relative paths from the tree
        root (not :lcd), escapes spaces, and refuses outside/symlink-escaping
        parents and candidates before directory traversal.

                                                     *:SimpleTreeClose*
:SimpleTreeClose
        Flush the current width state and close the current tab page's tree
        window, or every tree window when this tab page has none.

                                                     *:SimpleTreeDebug*
:SimpleTreeDebug
        Print root, window, backend, pending-request, cache state, and a compact
        rendering-statistics summary.

                                                     *:SimpleTreeStats*
:SimpleTreeStats[!]
        Report session-local render count, last/maximum/average duration,
        visible and actually rewritten lines, successful setbufline() /
        deletebufline() calls, subtree-cache hit/miss ratio, epoch
        invalidations, and discarded cache slices. With [!] reset only the
        counters. Reading or resetting never renders, advances the epoch, or
        invalidates a subtree, so observation does not manufacture misses.

                                                     *:SimpleTreeHealth*
:SimpleTreeHealth
        Check required Vim features, normalized configuration ranges, the
        backend executable, and detected trash and clipboard providers.

        Also reported: whether lib/simpletree-daemon is older than the Rust
        sources shipped beside it — the usual state after a plugin-manager
        update that did not rebuild, whose remedy is ./install.sh followed by
        |:SimpleTreeRestart|; the outcome of the most recent git-status query,
        including the backend's own error text when it failed; and the live
        session — root, tree windows, pending scans, in-flight callbacks and
        the first directory whose scan failed.  Git status is reported from
        that last outcome, not from the capability bit, so a root outside any
        repository now says why instead of claiming to be active.

                                                     *:SimpleTreeColumns*
:SimpleTreeColumns [{column}...]
        Set |g:simpletree_columns| for this session.  With no argument it
        toggles between "size mtime" and none; "none" (or "off") clears it.
        An unknown column name is refused and changes nothing.

                                                     *:SimpleTreeVersion*
:SimpleTreeVersion
        Run the discovered backend with --version and display the result.

                                      *:SimpleTreeToggleAutoRefresh*
:SimpleTreeToggleAutoRefresh
        Toggle |g:simpletree_auto_refresh| for the current Vim session.

                                       *:SimpleTreeToggleAutoFollow*
:SimpleTreeToggleAutoFollow
        Toggle |g:simpletree_auto_follow| for the current Vim session.

                                                *:SimpleTreeSearch*
:SimpleTreeSearch {query}
        Ask the backend for a recursive filename search below the current
        root and put the matches into the quickfix list.  Requires a backend
        that advertises the "search" capability.  Substring matching is the
        default; call simpletree#Search({query}, 'fuzzy') for ordered
        subsequence matching.  Only the newest search may write the quickfix
        list: starting another one cancels the previous walk, so a slow earlier
        search can no longer replace the results being read.

                                              *:SimpleTreeMarkClear*
:SimpleTreeMarkClear
        Clear every mark.  Same as gM in the tree.

                                            *:SimpleTreeStateClear*
:SimpleTreeStateClear
        Delete the persisted session state (the per-root expanded sets and the
        last root).  The tree on screen is untouched; the session it belongs to
        will record itself again when it closes.  See |simpletree-session-state|.

                                                     *:SimpleTreeSort*
:SimpleTreeSort [mode]
        Set the tree order to name, extension, mtime or size.  With no mode,
        cycle to the next one.  Directories always remain before files;
        mtime and size default to newest/largest first.

                                              *:SimpleTreeSortReverse*
:SimpleTreeSortReverse
        Reverse the current sort order while retaining directories first.

==============================================================================
5. MAPPINGS                                      *simpletree-mappings*

GLOBAL MAPPING ~

SimpleTree defines:                               *<Plug>(simpletree-toggle)*
>
  <Plug>(simpletree-toggle)
<
By default <Leader>e maps to this target only when the key is unused and
|g:simpletree_set_default_mapping| is enabled.

TREE-BUFFER MAPPINGS ~

<CR>, o, double-click
        Open a file or toggle a directory.

l, Right
        Expand a directory and enter its first child; open a file.

h, Left, <BS>
        Collapse the current directory or nearest expanded ancestor.

R       Refresh.
H       Toggle dotfiles.
I       Toggle Git ignore filtering.
s       Cycle name / extension / mtime / size sorting.
gs      Reverse the current sorting mode.
q       Close the tree.
e       Use the selected directory as root; a file uses its parent.
U       Move the root up one directory.
C       Prompt for a root directory.
.       Use Vim's current working directory as root.
d       Use the active file's directory as root.
L       Toggle root locking.
c       Copy the selected path, or the whole marked set, to SimpleTree's
        file clipboard.
x       Cut the selected path, or the whole marked set.
p       Paste into the selected directory, or a selected file's parent.  A
        successful paste clears the marks.
a, n    Create a file.
A, N    Create a directory.
r       Rename.
D       Delete, preferring the system trash when available.  With marks set,
        one confirmation covers the whole set; every path is then re-checked
        individually and the failures are reported together.
<Space> Mark or unmark the node under the cursor, then move down one line.
        In Visual mode the same key marks every node in the selection, or
        unmarks them when the selection is already fully marked.
gm      Mark every visible node sharing the cursor node's parent directory.
gM      Clear all marks.  Also available as |:SimpleTreeMarkClear|.
P       Preview while keeping tree focus.
V, <C-V>
        Open in a vertical split.
S, <C-X>
        Open in a horizontal split.
t, <C-T>
        Open in a new tab.
f       Reveal the active file.
y       Copy the basename to Vim's unnamed register and optionally the system
        clipboard.
Y       Copy the absolute path to Vim's unnamed register and optionally the
        system clipboard.
gx      Open with the system default application.
m       Toggle a bookmark on the node under the cursor.  Bookmarked entries
        are marked in the tree and persist across sessions.
'       List all bookmarks in the quickfix window.  Bookmarks pointing at
        paths that no longer exist are dropped here.
]b, [b  Jump to the next or previous bookmark visible in the tree, wrapping
        around.
z       Collapse all directories below the root.  Configure with
        |g:simpletree_collapse_all_key|.
F       Filter the tree by name; matching entries and the directories leading
        to them stay visible.  An empty query clears the filter.  Matches are
        found across the whole tree, including directories that have never
        been expanded — see |simpletree-filter|.
/       Prompt for a name and jump to the next matching row.
]f, [f  Jump to the next / previous match of the last / query.
?       Toggle the mapping help window.

The root is locked by default.  Press L before e, U, C, . or d when root
changes are needed.

CUSTOM MAPPINGS ~
                                                *g:simpletree_mappings*
Every tree-buffer key can be remapped or disabled through a dictionary of
{key: action} pairs.  An empty action disables the key: >
  let g:simpletree_mappings = {'X': 'refresh', 'q': ''}
<
Available actions: open, expand, collapse, collapse_all, refresh,
toggle_hidden, toggle_git_ignore, close, root_here, root_up, root_prompt,
root_cwd, root_current, toggle_root_lock, copy, cut, paste, new_file,
new_folder, rename, delete, help, open_vsplit, open_split, open_tab,
preview, reveal_active, yank_name, yank_path, system_open, filter, find,
find_next, find_prev, sort_cycle, sort_reverse, bookmark_toggle,
bookmark_list, bookmark_next, bookmark_prev, mark_toggle, mark_siblings,
mark_clear.

The mark_toggle action installs a Visual-mode mapping on the same key as its
Normal-mode one, so remapping it moves both.

|g:simpletree_collapse_all_key| is still honored as a compatibility alias
for the collapse_all action.

USER EVENTS ~
                                                *simpletree-events*
SimpleTree fires |User| autocommands around tree lifecycle and file
operations.  Payload data is passed through g:simpletree_event: >
  autocmd User SimpleTreeFileCreated echom g:simpletree_event.path
<
Events: SimpleTreeOpen, SimpleTreeClose, SimpleTreeRootChanged,
SimpleTreeNodeOpened, SimpleTreeDirExpanded, SimpleTreeDirCollapsed,
SimpleTreeFileCreated, SimpleTreeFileDeleted, SimpleTreeFileRenamed,
SimpleTreeGitStatusUpdated, SimpleTreeFilterChanged.

==============================================================================
6. OPTIONS                                        *simpletree-options*

Set options before plugin/simpletree.vim is loaded.  Boolean options use 0 or
1.

TREE, ROOT, AND REFRESH ~

                                             *g:simpletree_width*
g:simpletree_width
        Number, default 45.  Requested tree width, clamped to 10..500.

                                             *g:simpletree_persist_width*
g:simpletree_persist_width
        Boolean, default 1.  Persist manually adjusted tree width.

                                             *g:simpletree_width_state_file*
g:simpletree_width_state_file
        String.  Width-state path.  See |simpletree-width-state|.

                                      *g:simpletree_width_persist_delay*
g:simpletree_width_persist_delay
        Number, default 250.  Width-write debounce in milliseconds, clamped to
        0..5000.  Zero writes synchronously.

                                             *g:simpletree_show_root*
g:simpletree_show_root
        Boolean, default 1.  Display a collapsible workspace-root row.

                                             *g:simpletree_root_locked*
g:simpletree_root_locked
        Boolean, default 1.  Set the initial root-lock state.

                                             *g:simpletree_hide_dotfiles*
g:simpletree_hide_dotfiles
        Boolean, default 1.  Hide dotfiles.

                                             *g:simpletree_git_ignore*
g:simpletree_git_ignore
        Boolean, default 1.  Follow Git ignore rules.

                                             *g:simpletree_page*
g:simpletree_page
        Number, default 200.  Entries per response chunk, clamped to 1..1000.

                                             *g:simpletree_sort*
g:simpletree_sort
        String, default "name".  One of name, extension, mtime or size.  The
        latter two request entry metadata and sort newest/largest first.
        Metadata is retained with each cached directory snapshot, so later
        switches between sort modes do not rescan the same snapshot.

                                     *g:simpletree_sort_reverse*
g:simpletree_sort_reverse
        Boolean, default 0.  Reverse the current mode without moving files
        ahead of directories.

                                             *g:simpletree_auto_follow*
g:simpletree_auto_follow
        Boolean, default 1.  Reveal an ordinary file buffer entered in Vim.

                                   *g:simpletree_auto_follow_change_root*
g:simpletree_auto_follow_change_root
        Boolean, default 0.  Change root for an active file outside the tree.
        A locked root prevents this.

                                             *g:simpletree_auto_refresh*
g:simpletree_auto_refresh
        Boolean, default 1.  Master switch for automatic refresh checks.

                                    *g:simpletree_auto_refresh_on_focus*
g:simpletree_auto_refresh_on_focus
        Boolean, default 1.  Check expanded directories when Vim gains focus.

                                     *g:simpletree_auto_refresh_on_idle*
g:simpletree_auto_refresh_on_idle
        Boolean, default 1.  Check expanded directories on CursorHold.

                                    *g:simpletree_auto_refresh_interval*
g:simpletree_auto_refresh_interval
        Number, default 3000.  Minimum idle-refresh interval in milliseconds,
        clamped to 3000..600000.

OPENING FILES AND WINDOWS ~

                                             *g:simpletree_keep_focus*
g:simpletree_keep_focus
        Boolean, default 1.  Keep focus in the opened file.

                                             *g:simpletree_choose_window*
g:simpletree_choose_window
        Boolean, default 1.  Prompt when several editor windows are candidates.

                                             *g:simpletree_split_force_right*
g:simpletree_split_force_right
        Boolean, default 1.  Put a new vertical editor split on the right.

                                             *g:simpletree_split_below*
g:simpletree_split_below
        Boolean, default 1.  Put a horizontal split below its target window.

                                             *g:simpletree_open_on_create*
g:simpletree_open_on_create
        Boolean, default 1.  Open a newly created file in the editor.

DISPLAY AND ICONS ~

                                             *g:simpletree_show_modified*
g:simpletree_show_modified
        Boolean, default 1.  Mark modified, unsaved buffers.

                                             *g:simpletree_modified_symbol*
g:simpletree_modified_symbol
        String, default '●'.  Modified-buffer marker.

g:simpletree_show_bookmarks                          *g:simpletree_show_bookmarks*
  Show the bookmark marker in the tree.  Default 1.

g:simpletree_bookmark_symbol                        *g:simpletree_bookmark_symbol*
  Marker appended to a bookmarked entry.  Default "★".

g:simpletree_bookmarks_file                          *g:simpletree_bookmarks_file*
  Where bookmarks are stored.  Empty (the default) uses
  $XDG_STATE_HOME/simpletree/bookmarks.json, falling back to
  ~/.local/state/simpletree/bookmarks.json.  The file is a JSON array of
  absolute paths, so it can be edited or version-controlled by hand.

g:simpletree_mark_symbol                                *g:simpletree_mark_symbol*
  Marker appended to a marked entry.  Default "✓".  See |simpletree-marks|.

g:simpletree_persist_state                            *g:simpletree_persist_state*
  Remember which directories were expanded, per root, and restore them the
  next time that root is opened.  Default 1.  See |simpletree-session-state|.

g:simpletree_state_file                                  *g:simpletree_state_file*
  Where that state is stored.  Empty (the default) uses
  $XDG_STATE_HOME/simpletree/state.json, falling back to
  ~/.local/state/simpletree/state.json.

g:simpletree_state_max_roots                        *g:simpletree_state_max_roots*
  How many roots the file keeps, clamped to 0..1000.  Default 20.  When the
  limit is reached the least recently saved root is dropped; the root of the
  session doing the saving is never the one dropped.

g:simpletree_state_max_dirs                          *g:simpletree_state_max_dirs*
  How many expanded directories are recorded per root, clamped to 0..20000.
  Default 500.  Shallower directories are kept first: dropping an ancestor
  would keep its descendants from being shown anyway.

g:simpletree_restore_last_root                    *g:simpletree_restore_last_root*
  Make `:SimpleTree` with no argument reopen the root of the last session
  instead of the directory of the current file.  Default 0.

                                             *g:simpletree_use_nerdfont*
g:simpletree_use_nerdfont
        Boolean, default 1.  Use Nerd Font glyphs.

                                             *g:simpletree_show_file_icons*
g:simpletree_show_file_icons
        Boolean, default 1.  Display an icon based on file extension.

                                             *g:simpletree_folder_suffix*
g:simpletree_folder_suffix
        Boolean, default 1.  Display a slash suffix on directory names.

                                             *g:simpletree_icons*
g:simpletree_icons
        Dictionary, default {}.  Override dir, dir_open, file, and loading.

                                             *g:simpletree_file_icon_map*
g:simpletree_file_icon_map
        Dictionary, default {}.  Override file icons by lowercase extension.

                                                  *g:simpletree_columns*
g:simpletree_columns
        List, default [].  Detail columns rendered to the right of the name,
        in the given order.  Accepts 'size', 'mtime' and 'symlink'; unknown
        names are dropped.  Enabling any column makes directory listings ask
        the backend for metadata, exactly as sorting by mtime or size does, so
        turning them on may cost one re-scan.  Directories show "-" for size.
        The block is right-aligned to the tree width and carries the
        SimpleTreeColumn highlight group (`highlight default link` to Comment).

                                       *g:simpletree_column_time_format*
g:simpletree_column_time_format
        String, default '%m-%d %H:%M'.  |strftime()| format for the mtime
        column.

                                             *g:simpletree_column_sep*
g:simpletree_column_sep
        String, default '  '.  Separator between two detail columns.

                                             *g:simpletree_collapse_all_key*
g:simpletree_collapse_all_key
        String, default 'z'.  Key for collapsing all directories below root.

FILE OPERATIONS, BACKEND, AND DIAGNOSTICS ~

                                             *g:simpletree_use_trash*
g:simpletree_use_trash
        Boolean, default 1.  Prefer a supported system trash command.

                                             *g:simpletree_use_system_copy*
g:simpletree_use_system_copy
        Boolean, default 0.  Try an operating-system copy command first for
        ordinary files.  Only consulted by the in-Vim fallback; see
        |g:simpletree_async_fs_ops| and |simpletree-safety|.

                                             *g:simpletree_async_fs_ops*
g:simpletree_async_fs_ops
        Boolean, default 1.  Perform copy and move in the backend rather than
        in Vim, when the backend advertises the "fs-ops" capability.  See
        |simpletree-fsops|.  Set to 0 to force the in-Vim implementation,
        which blocks the editor for the whole duration of a copy.

                                     *g:simpletree_use_system_clipboard*
g:simpletree_use_system_clipboard
        Boolean, default 1.  After y or Y writes Vim's unnamed register, also
        try the + register or an external clipboard provider.

                                             *g:simpletree_daemon_path*
g:simpletree_daemon_path
        String, default ''.  Absolute backend path.  Empty searches runtimepath
        entries under lib/, target/release/, and target/debug/.

                                             *g:simpletree_debug*
g:simpletree_debug
        Boolean, default 0.  Emit additional messages visible in :messages.

                                             *g:simpletree_set_default_mapping*
g:simpletree_set_default_mapping
        Boolean, default 1.  Install <Leader>e only when it is unused.

                                             *g:simpletree_git_status*
g:simpletree_git_status
        Boolean, default 1.  Show per-file git status marks and colors.
        Effective only when the backend advertises the "git-status"
        capability and git is executable; otherwise the tree silently
        behaves as if this were 0.  See |simpletree-git-repos| for what
        happens when the root holds more than one repository, or sits inside
        a very large one.

REPOSITORY DISCOVERY ~
                                                   *simpletree-git-repos*
The tree root is not assumed to be a repository, nor to be inside exactly one.

When the root is inside a repository, status is asked for that repository and
restricted to the root's own subtree with a pathspec.  A tree rooted at
`<monorepo>/services/api` therefore does not re-run status over ten thousand
unrelated files on every save, and does not ship their statuses over the
protocol either.

When the root is not inside a repository, every repository *below* it is found
instead, so a directory of checkouts (`~/projects`, `~/work`) shows marks in
each one.  The search descends at most three levels, stops at the first
repository on each branch, ignores dotted directories and symbolic links, and
covers at most 32 repositories.  Each repository answers with its own event and
the frontend merges them; a repository that fails does not cost the others
their marks, and |:SimpleTreeHealth| reports how many were found.

A `.git` entry only counts as a repository when it is a file (a worktree or
submodule pointer) or a directory containing HEAD.  An empty `.git` directory
above your project no longer turns every status query into git's own "not a
git repository" error reported against your tree root.

When no repository is found at all, that is reported through
|:SimpleTreeHealth| rather than silently producing an unmarked tree.

                                             *g:simpletree_git_status_symbols*
g:simpletree_git_status_symbols
        Dictionary, default {}.  Override status symbols keyed by the codes
        M (modified), S (staged), U (untracked), C (conflict), D (deleted).
        Defaults follow |g:simpletree_use_nerdfont|.

        Highlight groups (all `highlight default link`):
        SimpleTreeGitModified, SimpleTreeGitStaged, SimpleTreeGitUntracked,
        SimpleTreeGitConflict, SimpleTreeGitDeleted.

                                             *g:simpletree_use_watcher*
g:simpletree_use_watcher
        Boolean, default 1.  Subscribe expanded directories to backend
        filesystem watching and refresh from pushed fs_event records.
        Directories whose watch registration fails, and backends without
        the "watch" capability, fall back to mtime polling.

==============================================================================
7. PERSISTED STATE                         *simpletree-width-state*

When |g:simpletree_persist_width| is enabled, a resize updates the in-session
width immediately.  Disk writes are debounced by
|g:simpletree_width_persist_delay|; only the latest resize is written.  Closing
the tree or exiting Vim forces a final write.

Default paths:

- $XDG_STATE_HOME/simpletree/width when $XDG_STATE_HOME is set.
- ~/.local/state/simpletree/width on Unix otherwise.
- ~/vimfiles/simpletree/width on Windows otherwise.

                                                *simpletree-session-state*
The set of expanded directories is persisted the same way, per root, in
|g:simpletree_state_file|.  It is written when the tree closes, when the root
changes and on |VimLeavePre|, and read back the first time a root is opened in
a session: reopening a project comes up with the directories you had open
rather than a bare root.

Restoring writes nothing to disk and starts no extra work.  Directories that
have since been deleted or moved out of the root are dropped, and a directory
you already touched in this session is never overwritten - collapsing one and
reopening the same root keeps it collapsed.

Only expansion is restored.  The tree root itself still follows the current
file unless |g:simpletree_restore_last_root| is set, and neither the sort mode,
the filter, the marks nor the hidden-file toggles survive a session.  The file
is JSON and safe to delete by hand; |:SimpleTreeStateClear| does it for you,
and :SimpleTreeHealth reports how many roots it holds.

==============================================================================
8. BACKEND DIAGNOSTICS                         *simpletree-backend*

The backend supports: >

  simpletree-daemon --version
  simpletree-daemon --help
  simpletree-daemon --self-test
<
--self-test scans a directory in-process and checks the handshake, then prints
"ok".  install.sh runs it before replacing a working backend, so a build that
compiles but cannot walk a directory never reaches lib/.  Every daemon in the
simple* suite answers these three the same way.

Protocol mode uses one JSON object per line.  A client may send: >

  {"type":"ping","id":1}
<
The pong event includes protocol_version (currently 2), daemon_version, and
capabilities.  Protocol v2 adds: a `meta` flag on list requests (size, mtime,
is_symlink per entry), watch/unwatch requests with pushed fs_event records,
git_status requests with per-file codes and directory aggregation,
streaming search requests, and fs_op requests (|simpletree-fsops|).
Unreadable children degrade to warnings on the final chunk instead of failing
the listing, and non-UTF-8 names are listed lossily with a non_utf8 flag.  Existing list and cancel requests remain
compatible; old clients ignore unknown event types.  Use
|:SimpleTreeVersion| for the executable discovered by Vim.

The daemon bounds concurrent blocking scans and keeps accepted requests
cancelable while waiting for a scan slot.  Protocol writes remain serialized;
queued records are coalesced before stdout is flushed.

==============================================================================
9. FILE-OPERATION SEMANTICS                       *simpletree-safety*

- The workspace root cannot be cut, renamed, or deleted from the tree.
- Nested create names must remain relative to the selected destination.
- A directory cannot be pasted into itself or its descendants.
- Write targets, rename, and delete are constrained by the resolved workspace
  root, including symbolic-link boundaries.
- A copied symbolic link is recreated as a link, never dereferenced.  The
  backend does this itself; the in-Vim fallback needs cp -a on Unix and refuses
  the copy when no safe provider exists.
- Delete always asks for confirmation and prefers a supported trash command.
- Copy, overwrite, atomic move, and rename use sibling staging or backup paths
  and attempt restoration after failure.
- Unsaved buffers associated with source, target, or a directory subtree block
  destructive operations.
- y and Y always write Vim's unnamed register first.

These protections reduce data-loss risk but are not a cross-platform
transaction or crash-consistency guarantee.  SimpleTree does not currently
provide undo for file operations.

BACKEND FILE OPERATIONS ~
                                                        *simpletree-fsops*
Copying used to move every byte through Vimscript on the main thread, so
pasting a large directory froze the editor — no redraw, no CTRL-C, no progress
— until it finished.  When the backend advertises the "fs-ops" capability and
|g:simpletree_async_fs_ops| is on, c/x + p instead send one fs_op request per
item and return immediately; the tree updates as each reply arrives.

Only the byte moving is delegated.  Workspace containment, the unsaved-buffer
refusal, the conflict prompts and the post-prompt re-validation described in
|simpletree-safety| all still run in Vim, and every prompt for a batch is
answered before the first transfer starts, so an operation in flight cannot be
interrupted by a question.  The backend applies the same sibling
staging/backup discipline, and never follows a symbolic link: a copied link is
recreated as a link, which is why cp -a is no longer needed for them.

Items are transferred one at a time, in the order they were marked.  A backend
that exits mid-batch fails the outstanding item with a message rather than
leaving the paste silently unfinished.  Delete and rename remain in Vim: both
are a single system call, not a walk.

Without the capability (an older backend, or the option off) the previous
in-Vim implementation runs unchanged, including
|g:simpletree_use_system_copy|.

TREE FILTER ~
                                                       *simpletree-filter*
F prompts for a substring and narrows the tree to the entries whose name
contains it, plus the directories on the way to them, which are expanded for
as long as the filter is active.  That expansion is a rendering decision only:
clearing the filter restores exactly the directories you had open, not the
ones the filter needed.

Matches come from the backend's recursive walk, the same one |:SimpleTreeSearch|
uses, so a filter finds a file in a directory you have never expanded.  Results
stream in, and the statusline shows filter:{query} followed by "…" while more
are still arriving, or by "(N+)" when the walk stopped at
|g:simpletree_filter_max_results|.  Retyping the filter supersedes the previous
walk; a chunk from an abandoned query cannot put its matches back on screen.
An explicit |:SimpleTreeRefresh| re-runs the walk, because a refresh means the
filesystem changed.

Set |g:simpletree_filter_mode| to 'loaded' for the historical behaviour, in
which the filter only inspects directories whose listing is already cached.
That is also the automatic fallback against a backend that does not advertise
the "search" capability, where the filter cannot see beyond what is loaded.

                                            *g:simpletree_filter_mode*
g:simpletree_filter_mode
        String, default 'auto'.  One of 'auto', 'daemon', 'loaded'.  'auto'
        uses the backend when it advertises "search" and falls back to
        'loaded' otherwise.

                                     *g:simpletree_filter_max_results*
g:simpletree_filter_max_results
        Number, default 500, clamped to 1..5000.  Upper bound on the matches a
        single filter collects.

MARKED NODES ~
                                                        *simpletree-marks*
Marks are a transient "the next operation applies to these" set, unlike
bookmarks, which are a persistent "I care about this path" set.  <Space> marks
the node under the cursor and moves down a line; in Visual mode the same key
marks the whole selection (and unmarks it when it is already fully marked).
gm marks every visible node sharing the cursor node's parent directory, and gM
clears the set.  The count appears in the tree's statusline as marked:N and
each marked line ends with |g:simpletree_mark_symbol|.

While anything is marked, c, x and D act on the whole marked set instead of the
node under the cursor; every other key is unaffected.  Delete asks once, listing
up to five names plus "(and N more)", and then re-runs every per-path guard
described in |simpletree-safety| individually — an unsaved buffer or a path that
changed while the prompt was open fails that item only, and the failures are
reported together.

The workspace root can never be marked.  Marks are cleared whenever the root
changes — by a tree key (., d, C, root-up), by auto-follow, or by |:SimpleTree|
{dir} from any tab page — and after a successful paste; a refresh keeps them but
drops any whose path no longer exists.

==============================================================================
10. TAB PAGES                                       *simpletree-tabpages*

One tree may be shown in several tab pages at once.  |:SimpleTree| always acts
on the current tab page: it opens a tree window there when the tab has none,
and closes only that window when it has one.  Every tab page shows the same
tree buffer, so the root, the expanded directories, the scan cache, marks and
Git status are shared and a second tab costs no extra scanning.

Because the tree is shared, |:SimpleTree| without an argument does not re-root
an already open tree when you open it in another tab; pass a directory or use
|:SimpleTreeReveal| to change what is shown.  Closing the last tree window ends
the session; closing one of several leaves the others working.

|:SimpleTreeClose| closes the current tab page's tree window, or every tree
window when the current tab page has none.  Width persistence
(|simpletree-width-state|) measures the tree window in the current tab page
only.

==============================================================================
11. TROUBLESHOOTING                      *simpletree-troubleshooting*

BACKEND NOT FOUND ~

Run install.sh, then check: >

  :SimpleTreeVersion
  :SimpleTreeHealth
<
A nonstandard backend can be selected with |g:simpletree_daemon_path|.

INLINE SCAN ERROR ~

Fix the permission, path, or backend problem, then press R or run
|:SimpleTreeRefresh|.  Failed directories stop automatic retry storms until an
explicit refresh or a real directory timestamp change.

WIDTH IS NOT SAVED ~

Check that the parent of |g:simpletree_width_state_file| is writable.  Setting
|g:simpletree_width_persist_delay| to zero is useful for synchronous diagnosis.

DEFAULT MAPPING IS MISSING ~

SimpleTree does not overwrite an existing <Leader>e mapping.  Inspect it with
:nmap <Leader>e or map |<Plug>(simpletree-toggle)| explicitly.


==============================================================================
DAEMON SUPERVISION                                    *simpletree-daemon*

The Rust daemon is managed by simplecore, the supervisor shared by the whole
simple* plugin suite. It is vendored into this repository at
autoload/simpletree/core.vim; that file is generated, do not edit it in place.

                                                  *simpletree-simplecore*
The bundle lives at https://github.com/beamiter/simplecore. Each plugin
carries its own copy so that installing this one alone is enough, and >

  make core-verify
<
checks every vendored file against the sha256 recorded in
.simplecore.manifest, failing the build when one has drifted. `make check`
depends on it. To change the supervisor, clone the bundle beside the plugins,
edit it there, and re-vendor: >

  git clone https://github.com/beamiter/simplecore ../.simplecore
  ../.simplecore/vendor.sh
<

What it guarantees:

- Liveness is always read from |job_status()|. job_start() returns a job
  object even when the exec itself failed, so a daemon that cannot start is
  never mistaken for one that is running.
- A superseded daemon's late exit callback cannot clear the state of the
  replacement that already took its place.
- Output still queued when you stop the daemon is discarded, so an explicit
  stop cannot be undone by an event that was already in flight.
- A daemon that dies is restarted automatically with exponential backoff.
  If it keeps crashing, the supervisor reports it once and stops trying
  rather than respawning forever; :SimpleTreeRestart re-arms it.
- Requests are correlated to replies by id and can time out, so a wedged
  daemon never leaves a callback hanging.

:SimpleTreeHealth
        Report daemon path, run state, negotiated protocol and capabilities,
        uptime, crash and restart counts, and whether the crash-loop breaker
        has tripped, alongside this plugin's own diagnostics.

                                                      *:SimpleTreeRestart*
:SimpleTreeRestart
        Stop the daemon and start a fresh one. Clears the crash-loop breaker,
        so this is the way back after repeated failures.

                                                      *:SimpleTreeLog*
:SimpleTreeLog
        Open the daemon transcript in a scratch buffer: start and exit events,
        restarts, protocol handshakes, send failures and stderr.

vim:tw=78:ts=8:noet:ft=help:norl:
